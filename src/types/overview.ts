export type MoneyKpisData = {
  totalIncome: number;
  totalExpense: number;
  incomeChange: number;
  expenseChange: number;
  balance: number;
  savingsPercentage: number;
  totalInvested: number;
};
